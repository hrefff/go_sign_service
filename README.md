# build
```
$ go build main.go
```

# use
```
$ ./main
No valid action specified
Usage of ./main:
  -gen-keys
    	Generate RSA keys
  -priv string
    	Private key file (default "private.pem")
  -pub string
    	Public key file (default "public.pem")
  -sig string
    	Signature file (default "signature.sig")
  -sign string
    	Sign a file
  -verify string
    	Verify a file
```
